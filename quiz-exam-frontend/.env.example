# Copy this file to .env and fill in your values.
# In local dev, leave VITE_API_BASE_URL empty — Vite proxies /api to localhost:8080 automatically.
# In production, set it to your backend URL (no trailing slash).

VITE_API_BASE_URL=
