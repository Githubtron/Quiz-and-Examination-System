# Server
server.port=8080

# DataSource — update credentials as needed
spring.datasource.url=jdbc:mysql://localhost:3306/quizexam?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=prabhat123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.open-in-view=false

# JWT
app.jwt.secret=QuizExamSuperSecretKeyThatIsAtLeast256BitsLong1234567890ABCDEF
app.jwt.expiration-ms=86400000

# CORS — allow React dev server
app.cors.allowed-origins=http://localhost:5173,http://localhost:3000

# Google Gemini AI — get a FREE key at https://aistudio.google.com/app/apikey
# Paste your key below after the = sign
app.gemini.api-key=AIzaSyDTbnwcrop2AeV07Hmsji2VgmelP-FYIdw

# File upload limits
spring.servlet.multipart.max-file-size=20MB
spring.servlet.multipart.max-request-size=20MB
